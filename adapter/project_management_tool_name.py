from enum import Enum


class ProjectManagementToolName(str , Enum):
    """
        ProjectManagementToolName is an enum that represents the project management tool name.
    """
    ASANA = "asana"
    JIRA = "jira" 
